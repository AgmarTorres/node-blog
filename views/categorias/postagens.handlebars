<h1> {{categoria.nome}} </h1>
{{#each postagens}}
<div class="card mt-4">
    <h3>{{titulo}} </h3>
    <p> {{descricao}}</p>
    <a href="/postagem/{{slug}}"> <button class="btnc btn-success"> Leia mais <button></a>
    <hr>
    <small> Data de pulicação: {{data}} </small>
</div>
</div>
{{else}}
<h5> Nenhuma postagem </h5>
{{/each}}